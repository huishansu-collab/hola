# 改这个文件再 push,就会启动 .github/workflows/tts.yml 生成语音(前提:对应的仓库 secret 已配置:OpenAI → OPENAI_API_KEY,千问 → DASHSCOPE_API_KEY,火山 → VOLC_TTS_API_KEY;provider 可选 openai / qwen / volc / cosy;only 可填说话人、台词 id,或 ambience 只生成环境音)。
# 生成结果由工作流自动提交回当前分支。
provider: cosy
case: morning
force: true
only: assistant
model:
run: 10
